/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    turbo: {
      root: '/Users/lukehalley/dev/personal/zera_chrt/webapp',
    },
  },
}

module.exports = nextConfig
